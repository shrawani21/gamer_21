// Define project specifications
const specs = {
  project: {
      // Name of the project
      name: "Dot and Boxes",
      // Default branch of the repository
      defaultBranch: "main",
      // URL of the GitHub repository
      repoUrl: "https://github.com/shrawani21/gamer_21.git",
  },
  contributing: {
      // Flag indicating whether contributing guidelines should be generated
      generate: true
  }
};
